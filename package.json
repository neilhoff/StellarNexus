{
  "name": "stellar_nexus",
  "version": "1.0.0",
  "description": "Stellar Nexus",
  "scripts": {
    "start": "npm-run-all --parallel dev:*",
    "dev:startClient": "cd client && quasar dev",
    "dev:startServer": "arc sandbox",
    "testDev": "npx cypress open --env CYPRESS_ENV=dev",
    "testStaging": "npx cypress open --env CYPRESS_ENV=staging"
  },
  "devDependencies": {
    "@architect/architect": "^10.14.1",
    "cypress": "^12.14.0",
    "jsonwebtoken": "^9.0.0",
    "npm-run-all": "^4.1.5"
  },
  "dependencies": {
    "@architect/functions": "^8.1.6"
  }
}
